# inumaki Project Memory

This file is the durable working memory for Camie Dev Console sessions in this repository.
It was auto-generated from a first-pass repository scan and should be refined as the project evolves.

## Product Overview
- > **Open-source, Windows-first internal voice productivity tool.**
- > Press a hotkey → speak → get polished text pasted into any app.
- ![Status](https://img.shields.io/badge/status-MVP-violet)
- TypeScript codebase

## Architecture
- Monorepo with `apps/web` (Next.js App Router marketing/download site plus unauthenticated `/api/process`), `apps/desktop` (Electron + Vite React), and `packages/shared` (shared TS types).
- OSS-only product: no hosted/internal auth mode, no NextAuth, no OAuth/SMTP magic links, no invite/admin system, and no PostgreSQL/Drizzle persistence in the web app.

## Commands
- `dev`: `pnpm run dev:web`
- `lint`: `pnpm -r lint`
- `test`: `pnpm -r test`
- `typecheck`: `pnpm -r typecheck`
- `dev:web`: `pnpm --filter @inumaki/web dev`
- `dev:desktop`: `pnpm --filter @inumaki/desktop dev`
- `build:web`: `pnpm --filter @inumaki/web build`
- `build:desktop`: `pnpm --filter @inumaki/desktop build`

## Conventions
- Package name is `inumaki-ai`.
- Prefer TypeScript-aware changes and keep typecheck healthy.
- Web app has no DB layer after the OSS auth cleanup. Treat docs or code references to Prisma/Drizzle/Postgres/NextAuth as stale unless reintroduced intentionally.

## Important Files
- Start inspection with `README.md`.
- Start inspection with `package.json`.

## Known Risks
- Local shell/env values can affect dev startup; ensure `NODE_ENV` is standard, `PORT` is free, and `OPENAI_API_KEY` is present only when exercising the current web `/api/process` path.
- Current check/build caveats: desktop `tsconfig.json` uses deprecated `moduleResolution: "node"` under TypeScript 6; web `lint` uses `next lint`.
- Desktop global-hotkey recording path WAS broken (main-process `AudioRecorder.stop()` returned `null`; nothing started renderer capture on the hotkey; `paste-text` wrote clipboard but never simulated Ctrl+V) — FIXED 2026-06-26 (commit `b9cda27`). See the "Desktop P0" section below.
- README/docs were rewritten for OSS-only/no-auth setup on 2026-06-28.
- GitHub has two public repos: `Camie-Tech/inumaki` is the cloud/OpenAI API repo in this checkout, while `Camie-Tech/inumaki-oss` is the local `whisper.cpp` OSS repo. Latest OSS release checked: `v0.1.1`, with bundled Electron API, whisper.cpp binary, and `ggml-base.en.bin`.
- Downloaded and extracted OSS release assets locally under `releases/inumaki-oss/`; runnable unpacked app is `releases/inumaki-oss/Inumaki-AI-0.1.1-win-unpacked/win-unpacked/Inumaki AI.exe`. Verified SHA-256 hashes against GitHub release metadata.
- Inumaki landing page lives in `apps/web/src/app/page.tsx`, public brand PNGs in `apps/web/public/brand`, and resolves latest OSS Windows downloads through `/api/download/latest`.
- LP was redesigned (2026-06) into a premium dark Apple-style marketing page: async server component (no client hooks), CSS-only motion via `ink-*` keyframes in `globals.css`, semantic `<dl>/<ol>/<kbd>`, focus-visible rings, `prefers-reduced-motion` support. Body font is Inter (added in `layout.tsx` as `--font-inter`, wired to `--font-sans`); Orbitron is reserved for the "INUMAKI" wordmark + step numerals, Syne for headings, IBM Plex Mono for labels/keycaps. Palette: bg `#0b0f14`, accent cyan `#00aeef`/`#42caff`, slate text.
- BRAND CAVEAT: the wordmark PNGs (`inumaki-wordmark-*-transparent.png`) have DARK text, so they are invisible on dark backgrounds — render "Inumaki" as live Orbitron text on dark sections instead of using the wordmark image. The icon `inumaki-icon-dark-transparent.png` reads fine on dark.
- `apps/web/next.config.mjs` now pins `turbopack.root` to the monorepo root. This fixes the local `pnpm dev` "Can't resolve 'tailwindcss'" failure caused by Turbopack misdetecting the workspace root (a stray `C:\Users\THIS PC\package-lock.json` parent lockfile). Local dev verified: `pnpm dev` serves `/` HTTP 200 with live `v0.1.1` release data and compiled Tailwind CSS.
- Vercel project `inumaki` under `daniel-lordsons-projects` is linked in `.vercel/project.json`, configured with root directory `apps/web`, and deployed at `https://inumaki-five.vercel.app`. Custom domain `https://inumaki.camie.tech` now resolves and is the public/canonical production URL.
- DEPLOY (2026-06-25): `main` was fast-forwarded to the LP redesign (`e282489`) and the redesign is LIVE in production — `https://inumaki-five.vercel.app` serves the new page with live `v0.1.1` release data.
- IMPORTANT deploy fact: pushing to `main` did NOT auto-trigger a Vercel build (Git auto-deploy is not firing for this project). Deploy manually with `vercel --prod --yes` from the repo root (CLI is authed as `camie-ace`, scope `daniel-lordsons-projects`). `.vercelignore` correctly excludes `releases/` (1.2 GB) so CLI uploads are ~50 KB. Remote build uses pnpm 9.15.9 via corepack and passes typecheck + build (~17s).

## SEO / LP / Roadmap (2026-06-25)
- Canonical site origin centralized in `apps/web/src/lib/site.ts` as `SITE_URL = process.env.NEXT_PUBLIC_SITE_URL ?? 'https://inumaki-five.vercel.app'`. Production Vercel env `NEXT_PUBLIC_SITE_URL=https://inumaki.camie.tech` is set, so live canonical/OG/sitemap URLs use the custom domain; the fallback keeps no-env builds crawlable.
- Technical SEO substrate added (App Router file conventions in `apps/web/src/app/`): `sitemap.ts`, `robots.ts` (disallows `/api/ /admin /dashboard /auth/`), `manifest.ts`, generated `opengraph-image.tsx` + `twitter-image.tsx` (next/og, 1200×630, `#0b0f14`; twitter re-exports OG). `layout.tsx` metadata overhauled: env `metadataBase`, `alternates.canonical`, title template, keyword-forward title/description, Twitter card, robots, applicationName/authors/creator/publisher/category, `viewport.colorScheme:'dark'`. JSON-LD `@graph` (SoftwareApplication + Organization + WebSite + FAQPage) rendered server-side in `page.tsx` from the live release version. noindex on `/auth/*` via new `auth/layout.tsx` and on `admin`/`dashboard` page metadata; `/api/download/latest` sends `X-Robots-Tag: noindex` (stays 302).
- `lib/github-release.ts` gained `INUMAKI_REPO_URL` + `getInumakiRepoStars()` (ISR-cached, fails soft to null) — repo now linked in nav/hero/privacy/footer with a live star chip.
- LP `page.tsx` rewrite (server-component-safe, design system preserved): H1 restructured so the tagline (not the opaque "Inumaki" wordmark) is the visual+SEO hero with a mono descriptor line; "Free" added to eyebrow; download de-risk microcopy + SmartScreen hint + null-asset fallback; secondary CTAs → GitHub; feature grid 3→6 cells; NEW sections — privacy deep-dive (mic→whisper.cpp→clipboard flow + audit-the-code CTA), honest "Inumaki vs cloud dictation (Wispr Flow)" comparison `<table>`, use-cases/personas, FAQ (`<details>`, 12 Q&As = FAQPage JSON-LD source of truth), system requirements, expanded 3-col footer. Over-claims softened to match shipping reality (no live streaming; Ctrl+V paste not yet implemented → "ready to paste"/"on the clipboard").
- Plans written to `docs/SEO-PLAN.md` and `docs/ROADMAP.md`. Roadmap benchmarked vs Wispr Flow; its #1 item is FIX-FIRST P0: the desktop main-process recording path (`AudioRecorder.stop()` returns null) and real Ctrl+V/text injection — do NOT marketing-push the comparison until the core loop reliably lands text at the cursor.
- VERIFIED: `pnpm --filter @inumaki/web typecheck` clean; `next build` exit 0 with `/sitemap.xml /robots.txt /manifest.webmanifest /opengraph-image /twitter-image` registered and `/` static (5m ISR). Dev-server curl confirmed all new sections + endpoints render and `/auth/signin`+`/dashboard` emit `noindex,nofollow`.
- Note: Google deprecated FAQ rich snippets 2026-05-07 — FAQPage JSON-LD kept for users + AI Overviews, not an accordion rich result; rich-result winners here are SoftwareApplication/Organization/WebSite.
- COMMITTED + DEPLOYED 2026-06-26: web SEO/LP work committed as `d2d178b` on `main` and deployed to Vercel prod via `vercel --prod --yes` (deployment READY, aliased to `https://inumaki.camie.tech`; live + verified on `https://inumaki-five.vercel.app`). `main` is ahead of `origin/main` (commits NOT pushed to GitHub; Vercel auto-deploy doesn't fire anyway). `NEXT_PUBLIC_SITE_URL` is unset in Vercel so canonical falls back to the Vercel URL (correct) — set it to `https://inumaki.camie.tech` once DNS resolves.

## Desktop P0 — core dictation loop fixed (2026-06-26, commit `b9cda27`)
- CRITICAL DISTINCTION: this checkout's `apps/desktop` is the CLOUD variant — the renderer (`useRecorder.ts`) posts base64 audio to the web `POST /api/process` (OpenAI-backed); there is NO local whisper.cpp here. The local whisper build is the separate `Camie-Tech/inumaki-oss` repo (not in this checkout). So roadmap item "P0.4 model bundling" applies to inumaki-oss, not here.
- P0.1 recording bridge fixed: the global hotkey never actually started renderer capture (main only sent the unconsumed `recording-state-change`) and `stopRecording()` errored on `AudioRecorder.stop()` returning null (never sent `process-audio`). Fix: `main.startRecording()` now sends a new `start-recording` IPC that `useRecorder` listens for (begins MediaRecorder); `stopRecording()` sends `process-audio` (renderer stops/encodes/POSTs). `AudioRecorder` is now a pure state tracker. New preload bridge `onStartRecording`.
- P0.2/P0.3 paste injection: new `apps/desktop/src/main/windows-input.ts` — `captureForegroundWindow()` (called at record-start) + `pasteText()` which writes the clipboard then simulates Ctrl+V via PowerShell Win32 (`AttachThreadInput`+`SetForegroundWindow`+`SendKeys ^v`) and restores the prior clipboard. Best-effort keystroke over a guaranteed clipboard write (degrades to today's manual-paste behavior). NO native deps added (uses bundled `powershell.exe`). `main.ts` `paste-text` handler now calls it.
- Desktop typecheck/build UNBLOCKED & green (was a long-standing blocker): `tsconfig.json` `ignoreDeprecations: "6.0"`; `store.ts` typed shim for electron-store v11 (node10 can't read its ESM `exports` types); new `src/renderer/src/vite-env.d.ts` (CSS module + `WebkitAppRegion`); `main.tsx` `@ts-ignore` on the relative CSS side-effect import. `pnpm --filter @inumaki/desktop typecheck` → exit 0; `pnpm --filter @inumaki/desktop exec vite build` → exit 0 (renderer + main 405 kB + preload).
- Renderer split note: main (CJS/`__dirname`) and renderer (wants bundler resolution) share one `tsconfig.json`; the relative-CSS `@ts-ignore` is a symptom. Proper future fix = split into `tsconfig.node.json` + `tsconfig.app.json`.

## Desktop v0.1.1 RELEASE (2026-06-26)
- Built + published a Windows test release: https://github.com/Camie-Tech/inumaki/releases/tag/v0.1.1 (assets: `Inumaki AI Setup 0.1.1.exe` installer + `Inumaki AI 0.1.1.exe` portable, ~104 MB each, unsigned → SmartScreen). Build: `pnpm --filter @inumaki/desktop build:win` (electron-builder 26.8.1, electron 41.2.1). electron-builder output dir is now `release/` (gitignored), NOT `dist/`.
- Release-prep fixes (commit `7647ca7`, pushed to `origin/main`): (1) FIXED packaged-renderer white-screen bug — prod now `mainWindow.loadFile(path.join(__dirname,'..','renderer','index.html'))` instead of the old `../../renderer` file:// URL that resolved one dir too high; (2) created `apps/desktop/assets/` (icon.png + tray-icon.png copied from `apps/web/public/brand/inumaki-icon-dark-transparent.png`, 1254² — were MISSING, broke the build + risked a Tray crash); (3) electron-builder `win.icon` = `assets/icon.png` (auto-ICO) + `directories.output: release`; (4) `store.ts` default `apiBase` → `https://inumaki-five.vercel.app` (was a placeholder domain); (5) version 0.1.0→0.1.1 + author/description.
- `origin/main` is now PUSHED and current (commits d2d178b web SEO, b9cda27 desktop P0, 7647ca7 release-prep). The cloud repo (`Camie-Tech/inumaki`) previously had only v0.1.0.
- STILL NOT verified: runtime on-device behavior (text landing in a real app via the global hotkey + mic). The desktop app now has no auth gate and can call `/api/process` without bearer tokens, but the loop still needs runtime validation with microphone + focused app.

## Desktop auto-update (electron-updater) + v0.1.2/v0.1.3 (2026-06-26)
- Auto-update is now WORKING via electron-updater against GitHub releases. Key enablers in `apps/desktop/package.json` build config: `publish: [{provider:github, owner:Camie-Tech, repo:inumaki}]` (bakes `app-update.yml` + emits `latest.yml`), and **space-free `artifactName`** (`Inumaki-AI-Setup-${version}.${ext}` / `Inumaki-AI-Portable-...`) — CRITICAL because GitHub renames spaces to dots, which would break the `latest.yml`→asset-name match and 404 the download. `win.verifyUpdateCodeSignature:false` (unsigned test builds; the machine has SOME signtool cert but flip this true with a real EV cert for production).
- `updater.ts` rewritten: `autoDownload=true`, background download, "Restart now to apply" dialog, notifications, 4h interval, resilient errors. Called from `main.ts` `whenReady` only when `!isDev`. New `get-app-version` IPC + StatusBar shows the running version (visible proof an update applied).
- IMPORTANT: only the INSTALLED **NSIS** build self-updates; the portable does not. Auto-update can't retro-fix an already-installed older build that lacked the publish config — users must install v0.1.2+ once, then it's automatic.
- RELEASE PROCESS for desktop updates: bump `apps/desktop/package.json` version → `pnpm --filter @inumaki/desktop exec vite build && pnpm --filter @inumaki/desktop exec electron-builder --win --publish never` → upload via `gh release create vX.Y.Z -R Camie-Tech/inumaki --target main` with assets **`Inumaki-AI-Setup.exe`** (STABLE, version-less name as of 2026-06-26 — see download note below), `Inumaki-AI-Portable.exe`, `Inumaki-AI-Setup.exe.blockmap`, and **`latest.yml`** (REQUIRED for the updater; newest release must be GitHub "Latest"). Each release upload ~208MB; build the next version only AFTER the prior upload finishes (electron-builder overwrites `release/latest.yml`).
- LANDING-PAGE DOWNLOAD (fixed 2026-06-26, commit `3701f09`, DEPLOYED): the LP was tracking `Camie-Tech/inumaki-oss` (stuck at v0.1.1) while active releases ship to `Camie-Tech/inumaki`. Now: `INUMAKI_RELEASE_REPO` default → `Camie-Tech/inumaki`; download buttons + `/api/download/latest` point DIRECTLY at GitHub's always-latest installer `https://github.com/Camie-Tech/inumaki/releases/latest/download/Inumaki-AI-Setup.exe` (constant `INUMAKI_DOWNLOAD_URL` in `github-release.ts`) — no GitHub API call, no rate limit. This requires the STABLE version-less asset name, so electron-builder `artifactName` was changed to `Inumaki-AI-Setup.${ext}`/`Inumaki-AI-Portable.${ext}`, and the stable `Inumaki-AI-Setup.exe` was uploaded to the existing v0.1.4 release. The GitHub-API path (`getLatestInumakiRelease`/`selectWindowsDownloadAsset`) is now DISPLAY-ONLY (version/size chips); the selector was hardened (prefers Setup, ignores blockmap/portable). Prod verified: chip shows v0.1.4, download resolves to the real binary.
- RESOLVED 2026-06-28 SEO/COPY MISMATCH: the LP and new SEO pages must market the local-first/no-account/no-cloud product, not a cloud/API product. Current SEO target is "free local open-source Windows voice-to-text" with whisper.cpp/on-device transcription, offline use, no account, no remote transcription, and auditable privacy. Indexable content pages: `/wispr-flow-alternative`, `/privacy`, `/help`; `sitemap.ts` lists all four public routes. `docs/SEO-PLAN.md` has a dated local-first correction banner. Verified with `corepack pnpm --filter @inumaki/web typecheck`, `corepack pnpm --filter @inumaki/web build`, targeted grep for cloud/API wording in marketing files, and HTTP copy checks on `http://localhost:3001`. Deployed to Vercel prod 2026-06-28 via `vercel --prod --yes`; latest prod deployment `https://inumaki-l4hr53f2x-daniel-lordsons-projects.vercel.app` is aliased to `https://inumaki.camie.tech`, with custom-domain canonical tags and sitemap verified. `/api/download/latest` redirects to GitHub latest installer and sends `X-Robots-Tag: noindex`. Vercel frozen install requires `pnpm-lock.yaml` to match `apps/web/package.json`; after auth/database package removal, regenerate with `corepack pnpm install --lockfile-only` before deploy.
- Releases live: v0.1.2 (auto-update enabled) + v0.1.3 (the test target that v0.1.2 updates to). Commits `96a4ae3` (auto-update feat) + `5b6e7b4` (v0.1.3 bump) pushed to `origin/main`.
- NEW APP ICON (v0.1.4, commit `bd5a578`): replaced the placeholder brand icon with the user-generated Inumaki logo (dog-face + cyan voice-waveform + "I" monogram). Source assets are in the user's Downloads as `ChatGPT Image Jun 23, 2026, 11_50_4* (1-4).png` — (2)=dark app tile (used), (4)=light tile, (1)/(3)=horizontal wordmarks. They are 1254² RGB (no alpha, light background). Processed (2) with `sharp` (trim light margin → square 1024² → rounded-corner transparency) into `apps/desktop/assets/icon.png` + `tray-icon.png`. v0.1.4 is GitHub "Latest", so installed v0.1.2/v0.1.3 auto-update to it. The wordmark variants (1)/(3) are available if a header/wordmark is wanted later, and the WEB favicon/OG (`apps/web` brand PNGs) could be refreshed with this logo too (not yet done).
- TOOLING NOTE: ImageMagick is NOT installed; no Higgsfield MCP is connected. For image processing use `sharp` via a throwaway script run with `node` from the repo root (cwd-based require resolution). CAUTION: `npm install` (not pnpm) inside this repo pollutes root `package.json`/`package-lock.json` and the pnpm `node_modules` — if it happens, `git checkout -- package.json package-lock.json` then `pnpm install` to reconcile. The `$env:TEMP` path uses an 8.3 short name (`THISPC~1`) that breaks PowerShell `Set-Location`; use full long paths.

## Listening HUD overlay (2026-06-30)
- Added a floating "Inumaki is listening" HUD shown during the GLOBAL-HOTKEY dictation flow (not the in-app record button). Driven entirely by the main process; the renderer overlay never captures audio or steals focus.
- Implementation: a second frameless/transparent/`alwaysOnTop`/`focusable:false`/click-through (`setIgnoreMouseEvents(true)`) `BrowserWindow` (300x84) created HIDDEN at startup (`createOverlayWindow` in `main.ts`) and revealed via `showInactive()` so it never takes focus from the app being dictated into. Positioned bottom-center of the display under the cursor (`screen.getDisplayNearestPoint`). Floats with `setAlwaysOnTop(true,'screen-saver')` + `setVisibleOnAllWorkspaces`.
- SINGLE-BUNDLE approach (no Vite multi-entry / electron-builder changes): the overlay window loads the same `index.html` with a `#overlay` hash; `renderer/src/main.tsx` branches on `location.hash` to render `<OverlayHUD>` (`renderer/src/overlay/OverlayHUD.tsx`) instead of `<App>`.
- State machine driven from main over a new `overlay-state` IPC channel (preload `onOverlayState`; shared types `OverlayState`/`OverlayStateChange`): `listening`→`processing`→`success|error`→`hiding`→hide. `startRecording` shows `listening`, `stopRecording` sends `processing`, the `process-audio-result` IPC handler sends `success`/`error` then `hideOverlay(delay)` fades it out. Gated by an `overlayFlowActive` flag so in-window recordings don't pop the HUD.
- Visuals match the desktop design system (purple `--accent`, red `--recording`, green/amber states; IBM Plex Mono labels): pulsing mic + glow ring + synthetic CSS equalizer while listening, spinner while processing, check/warning on result, live `m:ss` timer. Animations are CSS-only in the VISIBLE overlay window (deliberately avoids the `backgroundThrottling` trap of polling audio levels from the often-hidden main window). New keyframes (`hud-enter/exit/eq/pulse-ring`) + `prefers-reduced-motion` overrides in `index.css`.
- New store setting `showOverlay` (default true; schema+defaults in `store.ts`, toggle "Listening overlay" in `SettingsPanel` Behavior section); main checks `store.get('showOverlay', true)` before showing.
- VERIFIED: `pnpm --filter @inumaki/{shared,desktop} typecheck` clean, `vite build` exit 0, desktop `lint` 0 errors (only pre-existing warnings). NOT yet runtime-verified on-device (needs mic + focused app to confirm the pill renders and never steals focus). Files: `main.ts`, `preload.ts`, `store.ts`, `renderer/src/main.tsx`, `renderer/src/overlay/OverlayHUD.tsx`, `renderer/src/index.css`, `renderer/src/pages/SettingsPanel.tsx`, `packages/shared/src/index.ts`.

## Onboarding redesign — first-run wizard (2026-06-30)
- `apps/desktop/src/renderer/src/pages/OnboardingPanel.tsx` was rewritten from the single static "first-run setup" screen into a 4-step wizard (all components/hooks in-file, inline styles, no new files): **Welcome → Microphone → Shortcut → Style+finish**. App.tsx wiring (`onContinue` flips to MainPanel) and the prop signature `{ onContinue }` are unchanged. Window stays fixed 360×520 (≈360×476 usable under the 44px TitleBar); StepShell root is `height:calc(100% - 44px)`.
- MIC step: real Web Audio meter via a `useMicMeter(deviceId)` hook — `getUserMedia` + `AnalyserNode` (fftSize 64) drives a live 13-bar meter; verification latches when smoothed peak>0.28 sustains >120ms; **mandatory teardown** (stop tracks + close AudioContext + cancel rAF) on unmount and on deviceId change so the OS mic indicator clears. Handles denied / nodevice / OverconstrainedError (falls back to default and reconciles `microphoneId` to `'default'` via a `fellBack` flag). Button-triggered grant (no auto-fire). Device `<select>` only shown when >1 input.
- SHORTCUT step: reads the ACTUAL hotkey at runtime and uses `isWindowsAltHotkey()` (from `apps/desktop/src/shared/hotkeys.ts`) to teach **hold-to-talk** (Windows+Alt default) vs **press-to-toggle** (accelerators) with correct copy. The interactive "try it" trial detects the gesture via the REAL registration path — subscribing to `electronAPI.onStartRecording`/`onProcessAudio` (NOT raw renderer keydown, which can't reliably read the Windows key and which a registered global shortcut would swallow). 6s escape hatch enables the CTA so nobody is trapped; changing the combo calls `updateHotkey()` to re-register so the trial matches. NO main/preload changes were needed.
- STYLE step: 2×2 output-mode cards with example transforms (writes `defaultMode` eagerly), a live preview strip, and the raw API Server URL demoted to a collapsed **Advanced** disclosure (only place it appears). Finish writes `apiBase`/`defaultMode`/`microphoneId`/`onboardingComplete` then `onContinue()` after a 750ms "✓ All set".
- CSS: onboarding keyframes/classes added to `index.css` (`orb-rise`, `pop-in`, `fade-in-right`/`animate-step`, `ring-once`, `expand-in`, `key-hint-glow`, plus `.ob-primary/.ob-arrow/.ob-ghost/.ob-card/.ob-keycap` and a `prefers-reduced-motion` block). Separate from the overlay-HUD `hud-*` keyframes.
- The renderer can import the pure `apps/desktop/src/shared/hotkeys.ts` helpers (verified by `vite build`); tsconfig `include: src/**/*` covers it.
- STALE INCONSISTENCY (not yet fixed): `RecordButton.tsx` (hardcoded "Ctrl + Shift + Space" hint), `MainPanel.tsx` (renderer keydown checks ctrl+shift+space), and `SettingsPanel.tsx` (`HOTKEYS` array = legacy list) still reference the legacy hotkey and should be migrated to `shared/hotkeys` (DEFAULT_HOTKEY/HOTKEY_OPTIONS) for consistency with the Windows+Alt default. Onboarding already reads the real value.
- VERIFIED: `pnpm --filter @inumaki/desktop typecheck` clean + `vite build` exit 0 after a multi-agent adversarial review (6 confirmed findings applied: OverconstrainedError device reconcile, micVerified latch clear on device switch, CheckBadge `var(--bg)` via style, font scale to 10px, eyebrow tracking 0.1em, token-driven state-border via `color-mix`). NOT yet runtime-verified on-device (needs mic + focused app + the global hotkey).

## Recent Decisions
- Project memory is stored in `.camie/project-memory.md`, which is the canonical durable memory for this top-level T3 project.
- Read this file at the beginning of every Codex session before making Inumaki-specific assumptions.
- Update this file at the end of each substantive turn when durable Inumaki-specific information is learned.
- OSS AUTH REMOVAL (2026-06-28): this repo now treats OSS as the only mode. Removed NextAuth/Drizzle/Postgres/Nodemailer dependencies, auth API routes, preferences API, admin client, DB schema/migrations, desktop auth token plumbing, and `AuthPanel`. `/dashboard` is always public, `/auth/*` routes are informational/redirects only, `/admin` redirects/404s, and `POST /api/process` is unauthenticated.
- HOTKEY UPDATE (2026-06-30): default trigger is now `Windows+Alt` (modifier-only). Desktop uses `node-global-key-listener` in `apps/desktop/src/main/hotkey-registration.ts` for this combo because Electron `globalShortcut` requires a non-modifier key; other configured shortcuts still use `globalShortcut`. Old stored `Control+Shift+Space` values migrate to `Windows+Alt`, but that legacy accelerator remains as an internal fallback if the Windows key listener fails. `node-global-key-listener` is externalized in the desktop Vite main build and `WinKeyServer.exe` is `asarUnpack`ed for packaged builds. Verified with desktop typecheck/lint/Vite build, web typecheck/build, and NSIS package build.
- Web `typecheck` script is now `next typegen && tsc --noEmit` because Next 16 generates route types before plain `tsc` can run reliably from a clean checkout.
- `packages/shared/tsconfig.json` exists after the 2026-06-28 release cleanup; `pnpm -r typecheck` covers shared, desktop, and web.
- RELEASE v0.1.5 (2026-06-28): published https://github.com/Camie-Tech/inumaki/releases/tag/v0.1.5 from commit `0444e8c` with stable assets `Inumaki-AI-Setup.exe`, `Inumaki-AI-Portable.exe`, `Inumaki-AI-Setup.exe.blockmap`, and `latest.yml`. Production web deploy `https://inumaki-34ep9b25k-daniel-lordsons-projects.vercel.app` is aliased to `https://inumaki.camie.tech`; `/api/download/latest` redirects to GitHub latest installer and `/auth/signin?desktop=1` is no-auth informational copy.
- GitHub Actions are currently blocked by account billing lock, not repository build failures. `CI` and tag `Release` runs for `0444e8c` failed before any steps with "account is locked due to a billing issue"; use local/Vercel verification until billing is fixed.

## Memory Maintenance Rules
- Keep this file concise, factual, and repo-specific.
- Prefer stable facts over temporary task notes.
- Update this file when you learn something future sessions should know immediately.
